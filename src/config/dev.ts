/**
 * Dev-only: hide all backgrounds, character sprites, and gallery images.
 *
 * `true`  → images hidden (script-only dev mode)
 * `false` → images shown normally
 *
 * Only applies during `npm run dev`. Production builds always show images.
 */
export const DEV_HIDE_IMAGES = true;